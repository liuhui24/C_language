#include<stdio.h>
int main(void)
{
    float amount,tax,total;
    printf("Enter an amount: ");
    scanf("%f",&amount);
    tax = amount * 0.05;
    total = tax + amount;
    printf("With tax added: $%.2f",total);
    return 0;
}
